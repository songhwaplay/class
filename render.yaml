services:
  - type: web
    name: classroom-game-hub
    runtime: node
    plan: free
    rootDir: game-hub-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: 22.17.0
      - key: NODE_ENV
        value: production